export interface iPhoneLandingPageProps {
  imagePath: string;
  rightBanner?: string;
  leftBanner?: string;
  mainBanner?: string;
}
export interface LayoutProps {
  children: React.ReactNode;
  logoPath: string;
}